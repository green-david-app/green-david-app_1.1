
PATCH – login & timesheets (univerzální parsování)

Nahrajte `main.py` do kořene projektu (přepište existující) a redeploy.
- `/api/timesheets` bere JSON i form-data, názvy polí CZ/EN, a vrací srozumitelné chyby.
- Session cookies nastavené pro Render+Safari (SameSite=Lax, Secure).

Hotovo.

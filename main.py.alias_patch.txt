# REPLACEMENT SNIPPET for main.py – add this block anywhere below imports
# ---- Aliases for /gd/api/* (calendar uses these paths) ----
@app.route('/gd/api/jobs', methods=['GET','POST','PATCH','DELETE'])
def gd_api_jobs():
    return api_jobs()

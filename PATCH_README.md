# Patch summary
- Backend: tasks timestamps auto-migrace (bez DEFAULT), camelCase→snake_case, aliasy /gd/api/*, Notes API.
- Frontend: přidán `calendar-notes-patch.js`, do `calendar.html` doplněny elementy pro Poznámku (přidáno) a script include.

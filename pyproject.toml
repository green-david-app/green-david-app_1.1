[tool.ruff]
line-length = 120
target-version = "py310"
extend-exclude = [
  "__MACOSX",
  "app.db",
  "app.db-wal",
  "app.db-shm",
]

[tool.ruff.lint]
select = ["E", "F", "I"]
ignore = [
  "E501",  # line length handled by formatter; keep non-blocking
]
